# Bundled Python runtime
# On release, the python/ directory should contain:
#   python.exe — Portable Python distribution (downloaded from python.org on first run)
#   Scripts/    — pip and installed packages
#   requirements.txt — MarkItDown Python dependencies

# The bundled Python is NOT stored in git (too large).
# It is downloaded and set up automatically by PythonSetupManager on first run.
